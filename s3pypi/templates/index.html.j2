<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Package Index</title>
</head>
<body>
{%- for package in packages | sort -%}
    {%- for filename in package.files | sort %}
        {% set tagName = filename %}
        {% if '+' in filename %}
            {% set tagName = filename | replace('+','.') %}
        {% endif %}
    <a href="{{ tagName }}">{{ filename }}</a><br>
    {%- endfor %}
{%- endfor %}
</body>
</html>
